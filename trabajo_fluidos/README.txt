- Requisitos:

python 2.7
scipy

----
para instalar scypy en mac:
Aplications > Utilities > Terminal.app

run:
pip install scipy

Si error: pip command not found
> instalar python 2.7 --> https://www.python.org
Luego volver a intentar
----

- Modo de uso:

En Terminal
run:
python '(path to fille)'

example:
python /Users/ml/Desktop/trabajo_fluidos/trabajo_fluidos_calc_momento.py

Aplicar el mismo proceso apara el archivo de calculo de ecuacion (trabajo_fluidos_calc_poly.py)

- Luego de correr los programas podras encontrar los archivos con toda la info en la carpeta 'files'